version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci --prefer-offline --no-audit
    build:
      commands:
        - export NODE_OPTIONS="--max-old-space-size=5120"
        - export NODE_ENV=production
        - npm run build
  artifacts:
    baseDirectory: /dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*